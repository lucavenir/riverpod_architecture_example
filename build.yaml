targets:
  $default:
    builders:
      json_serializable:
        options:
          field_rename: snake
          explicit_to_json: true
          checked: true
      freezed:
        enabled: true
global_options:
  freezed:freezed:
    runs_before:
      - isar_generator:isar_generator
